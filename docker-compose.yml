services:
  shorkmini-build:
    build: .
    command:
      - --always-build
      - --is-debian
    environment:
      - HOST_GID=1000
      - HOST_UID=1000
    privileged: true
    volumes:
      - ./shorkmini:/var/shorkmini
