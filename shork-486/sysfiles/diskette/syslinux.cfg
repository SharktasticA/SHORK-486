DEFAULT @ID@

LABEL @ID@
    SAY Starting @DIST@... Grab a nice cuppa whilst this loads! :)
    KERNEL bzImage
    INITRD rootfs.cpio.xz
    APPEND root=/dev/ram rdinit=/sbin/init console=tty1 tsc=unstable loglevel=4 atkbd.extra=1
