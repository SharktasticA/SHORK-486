diff --git a/ao.c b/ao.c
index 4d9fc42..ee77e75 100644
--- a/ao.c
+++ b/ao.c
@@ -35,6 +35,10 @@
 #include <stdio.h>
 #include <string.h>
 #include <signal.h>
+#include <sys/soundcard.h>
+
+// Raw OSS file descriptor (-1 = currently open)
+int oss_fd = -1;
 
 void set_play_device(char *devicename)
 {
@@ -502,25 +506,25 @@ void open_ao_playdevice(struct mad_header const *header)
             } else opened++;
         }
 
-        if(!opened &&  options.opt & MPG321_USE_OSS)
+        if(!opened && options.opt & MPG321_USE_OSS)
         {
-            ao_option *ao_options = NULL;
-            int driver_id = ao_driver_id("oss");
-
-            if(options.device)
-                ao_append_option(&ao_options, "dsp", options.device);
-
-            if((playdevice=ao_open_live(driver_id, &format, ao_options))==NULL)
+            const char *dsp = options.device ? options.device : "/dev/dsp";
+            int fmt = AFMT_S16_LE;
+            int ch = MAD_NCHANNELS(header);
+            int sp = header->samplerate;
+            
+            oss_fd = open(dsp, O_WRONLY);
+            if (oss_fd >= 0)
             {
-                if (options.device)
-                {
-                    fprintf(stderr, "Can't open libao driver with device %s (is device in use?)\n",
-                        options.device);
-                    exit(1);
-                }
-                else
-                options.opt |= MPG321_USE_SUN;
-            } else opened++;
+                int frag = 0x0004000a;
+                if (ioctl(oss_fd, SNDCTL_DSP_SETFRAGMENT, &frag) < 0)
+                    perror("SETFRAGMENT");
+                ioctl(oss_fd, SNDCTL_DSP_SETFMT, &fmt);
+                ioctl(oss_fd, SNDCTL_DSP_CHANNELS, &ch);
+                ioctl(oss_fd, SNDCTL_DSP_SPEED, &sp);
+                playdevice = (ao_device *)&oss_fd;
+                opened++;
+            }
         }
 
         if(!opened && options.opt & MPG321_USE_SUN)
@@ -588,7 +592,7 @@ void open_ao_playdevice(struct mad_header const *header)
 ao_device *open_ao_playdevice_buffer(struct mad_header const *header)
 {
         ao_sample_format format;
-	ao_device *pldev = NULL;
+        ao_device *pldev = NULL;
 
         /* Because these can sometimes block, we stop our custom signal handler,
            and restore it afterwards */
@@ -797,25 +801,25 @@ ao_device *open_ao_playdevice_buffer(struct mad_header const *header)
             } else opened++;
         }
 
-        if(!opened &&  options.opt & MPG321_USE_OSS)
+        if(!opened && options.opt & MPG321_USE_OSS)
         {
-            ao_option *ao_options = NULL;
-            int driver_id = ao_driver_id("oss");
-
-            if(options.device)
-                ao_append_option(&ao_options, "dsp", options.device);
+            const char *dsp = options.device ? options.device : "/dev/dsp";
+            int fmt = AFMT_S16_LE;
+            int ch = format.channels;
+            int sp = format.rate;
 
-            if((pldev=ao_open_live(driver_id, &format, ao_options))==NULL)
+            oss_fd = open(dsp, O_WRONLY);
+            if (oss_fd >= 0)
             {
-                if (options.device)
-                {
-                    fprintf(stderr, "Can't open libao driver with device %s (is device in use?)\n",
-                        options.device);
-                    return NULL;
-                }
-                else
-                options.opt |= MPG321_USE_SUN;
-            } else opened++;
+                int frag = 0x0004000a;
+                if (ioctl(oss_fd, SNDCTL_DSP_SETFRAGMENT, &frag) < 0)
+                    perror("SETFRAGMENT");
+                ioctl(oss_fd, SNDCTL_DSP_SETFMT, &fmt);
+                ioctl(oss_fd, SNDCTL_DSP_CHANNELS, &ch);
+                ioctl(oss_fd, SNDCTL_DSP_SPEED, &sp);
+                pldev = (ao_device *)&oss_fd;
+                opened++;
+            }
         }
 
         if(!opened && options.opt & MPG321_USE_SUN)
diff --git a/configure b/configure
index d0609a6..e5d0393 100755
--- a/configure
+++ b/configure
@@ -2600,7 +2600,7 @@ fi
 
 # Define the identity of the package.
  PACKAGE=mpg321
- VERSION=0.3.2
+ VERSION=0.3.2-shork
 
 
 cat >>confdefs.h <<_ACEOF
diff --git a/configure.ac b/configure.ac
index bfd1d54..b431366 100644
--- a/configure.ac
+++ b/configure.ac
@@ -20,7 +20,7 @@ AC_PREREQ(2.59)
 AC_INIT
 AC_CONFIG_SRCDIR([mad.c])
 AC_CONFIG_HEADER([config.h])
-AM_INIT_AUTOMAKE(mpg321, 0.3.2)
+AM_INIT_AUTOMAKE(mpg321, 0.3.2-shork)
 
 # Checks for programs.
 AC_PROG_AWK
diff --git a/fbuffer.c b/fbuffer.c
index aff9d70..af26ef5 100644
--- a/fbuffer.c
+++ b/fbuffer.c
@@ -21,8 +21,9 @@
 #include <sys/types.h>
 #include <unistd.h>
 #include <signal.h>
-#include <ao/ao.h>
 #include <sys/sem.h>
+#include <unistd.h>
+#include <errno.h>
 
 #include "mpg321.h"
 
@@ -30,12 +31,20 @@ ao_device *play_device = NULL;
 extern int quit_now;
 extern int stop_playing_file;
 extern int buffer_size;
+extern int oss_fd;
 signed long total_played_frames = 0;
 
-void output_sighandler (int sig)	{
-	ao_close (play_device);
-	ao_shutdown ();
-	exit (EXIT_SUCCESS);
+void output_sighandler(int sig)
+{
+	if (oss_fd >= 0)
+	{
+		close(oss_fd);
+		oss_fd = -1;
+	}
+	else
+		ao_close(play_device);
+	ao_shutdown();
+	exit(EXIT_SUCCESS);
 }
 
 void frame_buffer_p(){
@@ -75,13 +84,23 @@ void frame_buffer_p(){
 				kill (ppid, SIGUSR1);
 				handle_signals (SIGUSR1);
 			}
-		} else if ((channels != MAD_NCHANNELS(header) || rate != header->samplerate) && playdevice_is_live()){
-			ao_close(play_device);
+		}
+		else if ((channels != MAD_NCHANNELS(header) || rate != header->samplerate) && playdevice_is_live())
+		{
+			if (oss_fd >= 0)
+			{
+				close(oss_fd);
+				oss_fd = -1;
+			}
+			else
+				ao_close(play_device);
+
 			channels = MAD_NCHANNELS(header);
 			rate = header->samplerate;
 			play_device = open_ao_playdevice_buffer(header);
-			if (!play_device)	{
-				fprintf(stderr,"Unable to open play device");
+			if (!play_device)
+			{
+				fprintf(stderr, "Unable to open play device");
 				kill (ppid, SIGUSR1);
 				handle_signals (SIGUSR1);
 			}
@@ -117,7 +136,24 @@ void frame_buffer_p(){
 			semop(semarray,&end_sops,1);
 			continue;
 		}
-		ao_play(play_device, oframe->data, oframe->length);
+
+		if (oss_fd >= 0)
+		{
+			ssize_t off = 0, n;
+			while (off < oframe->length)
+			{
+				n = write(oss_fd, (char *)oframe->data + off, oframe->length - off);
+				if (n <= 0)
+				{
+					if (errno == EINTR) continue;
+					break;
+				}
+				off += n;
+			}
+		}
+		else
+			ao_play(play_device, oframe->data, oframe->length);
+
 		total_played_frames+=1;
 		dframes->total_played_frames += 1;
 		output_buffer_position = getnext_place(output_buffer_position);
diff --git a/mad.c b/mad.c
index ddc0a2e..9349a8b 100644
--- a/mad.c
+++ b/mad.c
@@ -964,15 +964,23 @@ enum mad_flow output(void *data,
 	    {
 	    	    channels = MAD_NCHANNELS(header);
 	    	    rate = header->samplerate;
-	    	    open_ao_playdevice(header);        
+	    	    open_ao_playdevice(header);
 	    }
 	    else if ((channels != MAD_NCHANNELS(header) || rate != header->samplerate) && playdevice_is_live())
 	    {
-	    	    ao_close(playdevice);
-	    	    channels = MAD_NCHANNELS(header);
-	    	    rate = header->samplerate;
-	    	    open_ao_playdevice(header);        
-	    }        
+            extern int oss_fd;
+            if (oss_fd >= 0)
+            {
+                close(oss_fd);
+                oss_fd = -1;
+            }
+            else
+                ao_close(playdevice);
+
+            channels = MAD_NCHANNELS(header);
+            rate = header->samplerate;
+            open_ao_playdevice(header);
+        }
     }
 
     static int peak_rate = 0;
@@ -1016,7 +1024,11 @@ enum mad_flow output(void *data,
 		(Output_Queue+mad_decoder_position)->length = pcm->length * 4;
 	}else
 	{
-		ao_play(playdevice, stream, pcm->length * 4);
+        extern int oss_fd;
+        if (oss_fd >= 0)
+            write(oss_fd, stream, pcm->length * 4);
+        else
+            ao_play(playdevice, stream, pcm->length * 4);
 	}
     }
     
@@ -1048,7 +1060,11 @@ enum mad_flow output(void *data,
 		(Output_Queue+mad_decoder_position)->length = pcm->length * 4;
 	}else
 	{
-		ao_play(playdevice, stream, pcm->length * 4);
+		extern int oss_fd;
+        if (oss_fd >= 0)
+            write(oss_fd, stream, pcm->length * 4);
+        else
+            ao_play(playdevice, stream, pcm->length * 4);
 	}
     }
         
@@ -1076,7 +1092,11 @@ enum mad_flow output(void *data,
 		(Output_Queue+mad_decoder_position)->length = pcm->length * 2;
 	}else
 	{
-		ao_play(playdevice, stream, pcm->length * 2);
+		extern int oss_fd;
+        if (oss_fd >= 0)
+            write(oss_fd, stream, pcm->length * 2);
+        else
+            ao_play(playdevice, stream, pcm->length * 2);
 	}
     }
 
diff --git a/mpg321.c b/mpg321.c
index 19282bb..c2edb8c 100644
--- a/mpg321.c
+++ b/mpg321.c
@@ -36,6 +36,7 @@
 #include <math.h>
 #include <libgen.h>
 #include <signal.h>
+#include <ao/ao.h>
 #include <limits.h>
 #include <sys/time.h>
 #include <assert.h>
@@ -61,6 +62,7 @@
 
 /* POSIX threads */
 #include <pthread.h>
+#include <errno.h>
 #include <semaphore.h>
 
 
@@ -581,6 +583,7 @@ int main(int argc, char *argv[])
 		    perror("Cannot obtain resources for semaphores");
 		    exit(EXIT_FAILURE);
 	    }
+
 	    semarray = semget(sem_key,3,IPC_CREAT | IPC_EXCL | S_IRWXU);
 	    if(semarray == -1)
 	    {
@@ -616,7 +619,16 @@ int main(int argc, char *argv[])
 	    }
 
 	    /* Shared Memory */
-	    mem_key = ftok(argv[0],1);
+	    if(access(argv[0],X_OK) == 0)
+            mem_key = ftok(argv[0],1);
+        else
+            mem_key = ftok(MPG321_PATH,1);
+        if(mem_key == -1)
+        {
+            perror("Cannot obtain resources for shared buffer");
+            goto out;
+        }
+
 	    shm_id = shmget(mem_key,buffer_size * sizeof(output_frame), IPC_CREAT | S_IREAD | S_IWRITE);
 	    if(shm_id == -1)
 	    {
@@ -624,6 +636,7 @@ int main(int argc, char *argv[])
 		    goto out;
 		    //exit(EXIT_FAILURE);
 	    }
+
 	    Output_Queue = shmat(shm_id,NULL,0);
 	    if(*(int *)Output_Queue == -1)
 	    {
@@ -642,8 +655,16 @@ int main(int argc, char *argv[])
 		    (Output_Queue+n)->seconds = 0;
 		    (Output_Queue+n)->num_frames = 0;
 	    }
-	    
-	    frames_key = ftok(argv[0],2);
+
+        if(access(argv[0],X_OK) == 0)
+            frames_key = ftok(argv[0],2);
+        else
+            frames_key = ftok(MPG321_PATH,2);
+        if(frames_key == -1)
+        {
+            perror("Cannot obtain resources for shared frames counter");
+            goto out;
+        }
 	    frames_id = shmget(frames_key,buffer_size * sizeof(decoded_frames), IPC_CREAT | S_IREAD | S_IWRITE);
 	    if(frames_id == -1)
 	    {
@@ -660,25 +681,23 @@ int main(int argc, char *argv[])
 		    goto out;
 		    //exit(EXIT_FAILURE);
 	    }
-	    	
-	    Decoded_Frames->is_http = 0;
-	    Decoded_Frames->is_file = 0;
+
+        memset(Decoded_Frames, 0, buffer_size * sizeof(decoded_frames));
+        Decoded_Frames->is_http = 0;
+        Decoded_Frames->is_file = 0;
 
     }
     else {
 	    ao_initialize();
 	    check_default_play_device();
     }
-    
+
     if (!(options.opt & MPG321_REMOTE_PLAY))
     {
         handle_signals(-1); /* initialize signal handler */
         remote_input_buf[0] = '\0';
     }
-    
-    if (!(options.opt & MPG321_QUIET_PLAY)) 
-        mpg123_boilerplate();
-    
+
     if (options.opt & MPG321_REMOTE_PLAY)
     {
         printf ("@R MPG123\n");
@@ -1152,10 +1171,16 @@ fall_back_to_read_from_fd:	//FIXME. Reported that on some embedded systems with
 
     if(!(options.opt & MPG321_ENABLE_BUFFER))
     {
-  	     if(playdevice)
-		     ao_close(playdevice);
-	     ao_shutdown();
-     }
+        extern int oss_fd;
+        if (oss_fd >= 0)
+        {
+            close(oss_fd);
+            oss_fd = -1;
+        }
+        else if (playdevice)
+            ao_close(playdevice);
+        ao_shutdown();
+    }
 
      if (!(options.opt & MPG321_REMOTE_PLAY))
      {
diff --git a/mpg321.h b/mpg321.h
index 798bff0..39fc488 100644
--- a/mpg321.h
+++ b/mpg321.h
@@ -36,13 +36,13 @@
 #include <sys/types.h>
 #include <stdio.h>
 #include <limits.h>
-#include <ao/ao.h>
+#include <ao/ao.h> 
 #include <mad.h>
 #include <semaphore.h>
 
 
-#define FAKEVERSION "0.3.2-1"
-#define VERSIONDATE "2012/03/25"
+#define FAKEVERSION "0.3.2-shork"
+#define VERSIONDATE "2026/07/07"
 
 #ifndef PATH_MAX
 #define PATH_MAX	4096
@@ -200,6 +200,7 @@ void check_ao_default_play_device();
 void check_default_play_device();
 int playdevice_is_live();
 void open_ao_playdevice(struct mad_header const *header);
+ao_device *open_ao_playdevice_buffer(struct mad_header const *header);
 
 /* remote control (-R) functions */
 void remote_get_input_wait(buffer *buf);
